# Current Task: Layer 2: The Database Foundation

## Objective
Executing "Layer 2: The Database Foundation" from the Master Plan.
1. Profile Customization (`migrations.py`): Add `profile_background`, `accent_color` to `employee_profiles`.
2. Role Syncing (`migrations.py`): Add `role_id_clocked_in`, `role_id_clocked_out`, `enable_role_sync` to `guild_settings`.
3. Limits & Errors (`migrations.py`): Add `max_shift_hours` to `guild_settings`, create `error_logs` table.
4. Timezone Enforcement (`migrations.py`): Enforce `DEFAULT 'America/New_York'` on `timezone`.

## Status
- **COMPLETED**: Layer 2: The Database Foundation
- All 4 database schema implementations are finalized in `migrations.py`.
- Working files unlocked. Ready for Layer 3.
